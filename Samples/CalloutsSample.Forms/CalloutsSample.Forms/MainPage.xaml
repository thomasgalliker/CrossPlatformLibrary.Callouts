<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CalloutsSample.Forms.MainPage"
             Title="CrossPlatformLibrary.Callouts">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <Button Grid.Row="0"
            Text="Show simple content"
            Clicked="SimpleCalloutButtonClicked"
            VerticalOptions="Start"
            HorizontalOptions="StartAndExpand"/>

    <Button Grid.Row="1"
           Text="Show content callout"
           Clicked="ContentCalloutButtonClicked"
           VerticalOptions="Start"
           HorizontalOptions="StartAndExpand"/>
  </Grid>

</ContentPage>